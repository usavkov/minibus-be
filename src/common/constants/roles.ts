// TODO: store roles in database
export enum Role {
  user = 'user',
  staff = 'staff',
  driver = 'driver',
  owner = 'owner',
  organization = 'organization',
  support = 'support',
  admin = 'admin',
}

export const ROLES_KEY = 'roles';
